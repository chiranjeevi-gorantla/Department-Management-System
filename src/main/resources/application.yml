server:
  port: 8081
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: rootadmin
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/dcpapp
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true